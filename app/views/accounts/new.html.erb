<% provide :title, t("accounts.sign_up") %>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <h1 class = "sign-up-title"><%= upercase t("accounts.sign_up") %></h1>
    <%= form_for @account do |f| %>
      <%= render "form", f: f %>
    <% end %>
  </div>
</div>
